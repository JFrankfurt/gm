{
  "name": "gm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npm --workspaces run test",
    "postinstall": "npm -w packages/shared run build",
    "dev": "npm -w packages/shared run build && concurrently -n shared,server,web -c magenta,blue,green \"npm -w packages/shared run dev\" \"npm -w apps/server run dev\" \"npm -w apps/web run dev\"",
    "build": "npm -w packages/shared run build && npm -w apps/server run build && npm -w apps/web run build",
    "lint": "npm --workspaces run lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}